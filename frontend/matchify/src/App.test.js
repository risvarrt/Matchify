import { render, screen } from '@testing-library/react';
// import App from './App';

test('renders matchify home page', () => {
  // render(<App />);
  // const linkElement = screen.getByText(/Welcome to My Matchify/i);
  // expect(linkElement).toBeInTheDocument();
});
